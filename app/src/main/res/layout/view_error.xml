<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/tvErrorMessage"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="@dimen/one_offset"
        android:src="@drawable/ic_cloud_off_black_96dp" />

    <TextView
        android:id="@+id/tvErrorMessage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:textAppearance="@style/TextAppearance.AppCompat.Body2"
        tools:showIn="@layout/activity_main"
        tools:text="Network error" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/tvErrorMessage"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="@dimen/one_offset"
        android:text="@string/touch_to_retry"
        android:textAppearance="@style/TextAppearance.AppCompat.Body1"
        android:textColor="?android:textColorSecondary"
        tools:showIn="@layout/activity_main" />

</RelativeLayout>